import { PlanWorkspace } from "@/components/plan/PlanWorkspace";

export const metadata = {
  title: "여행 설계 | TripNode",
  description: "지도 위에서 직접 여행 일정을 설계하세요",
};

export default function PlanPage() {
  return <PlanWorkspace />;
}
